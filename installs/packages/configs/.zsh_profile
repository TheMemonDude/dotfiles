export XDG_CONFIG_HOME=$HOME/.config
VIM="nvim"

alias vim="nvim"

alias g="git"
alias gc="g clone"
alias gs="g status"
alias gi="g init"
alias ga="g add"
alias gct="g commit"
alias gph="g push"
alias gpl="g pull"
alias gr="git restore"
alias gb="git branch"
alias gco="git checkout"

alias doc="docker"
alias lzd='lazydocker'

alias livebook="cd ~/livebook && MIX_ENV=prod mix phx.server"
# alias cursor="/opt/cursor.appimage"

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

export GIT_EDITOR=$VIM
export DEV_CODE="$HOME/code"
export PATH="$HOME/.cache/rebar3/bin:$PATH"
export ELIXIR_ERL_OPTIONS="-epmd_module Elixir.Livebook.EPMD"

# Where should I put you?
# bindkey -s ^f "tmux-sessionizer\n"

eval "$(~/.local/bin/mise activate zsh)"

export FLYCTL_INSTALL="/home/$USER/.fly"
export PATH="$FLYCTL_INSTALL/bin:$PATH"

# opencode
export PATH=/home/thememondude/.opencode/bin:$PATH

# go bin
export PATH=$PATH:/usr/local/go/bin
